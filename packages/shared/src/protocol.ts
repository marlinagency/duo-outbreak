export const ROOM_NAME = "duo_outbreak";

export const NET_MESSAGES = {
  input: "input",
  playerMeta: "playerMeta",
} as const;

export const ROOM = {
  maxPlayers: 2,
  simulationHz: 20,
  inputHz: 15,
} as const;
