services:
  - type: web
    name: duo-outbreak
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: corepack enable && pnpm install --frozen-lockfile --prod=false && pnpm build
    startCommand: pnpm start:public
    envVars:
      - key: SERVE_CLIENT
        value: "1"
