# Phase 2 server plan

The server package is deliberately inert in Phase 1.

Phase 2 will add a Colyseus room with two player slots and input/state sync.
Phase 3 will move the local wave, zombie, weapon, pickup, collision and scoring
rules into fixed-timestep authoritative systems.
